{
  "name": "initiative-app-kit",
  "version": "0.21.0",
  "description": "The toolkit for apps that act in an Initiative community: app keys, installation and member tokens, verifying Initiative's calls and webhooks, and manifest validation.",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Morelitea/initiative-app-kit.git"
  },
  "homepage": "https://github.com/Morelitea/initiative-app-kit#readme",
  "bugs": "https://github.com/Morelitea/initiative-app-kit/issues",
  "keywords": [
    "initiative",
    "app",
    "oauth2",
    "private-key-jwt",
    "jwt",
    "jwks",
    "webhook",
    "manifest"
  ],
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./schemas/app-manifest.json": "./schemas/app-manifest.json",
    "./manifest.contract.json": "./manifest.contract.json"
  },
  "bin": {
    "initiative-app": "./dist/cli.js"
  },
  "files": [
    "dist",
    "schemas",
    "manifest.contract.json",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepare": "npm run generate && npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build",
    "generate": "node scripts/generate.mjs",
    "check:generated": "node scripts/generate.mjs --check"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "ajv": "^8.20.0"
  }
}
